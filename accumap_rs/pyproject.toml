[build-system]
requires      = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name             = "accumap-rs"
version          = "0.1.0"
requires-python  = ">=3.8"

[tool.maturin]
features    = ["pyo3/extension-module"]
module-name = "accumap_rs"
